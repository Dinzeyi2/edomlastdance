# Core API
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-multipart>=0.0.12

# DB: async driver for the API process, sync driver for the Celery worker
# process (Celery tasks are synchronous; see app/db/session.py)
sqlalchemy>=2.0
asyncpg>=0.30
psycopg2-binary>=2.9
aiosqlite>=0.20

# Jobs
celery>=5.4
redis>=5.0

# Imagery / CV (rule-based fallback + image IO -- NOT ultralytics/sam2, see
# app/services/defects.py and app/services/footprint.py for why those are
# deliberately left as lazy/optional imports instead of base dependencies)
pillow>=10.4
numpy>=1.26
opencv-python-headless>=4.10
shapely>=2.0

# External calls
httpx>=0.27
boto3>=1.35

# Optional LLM reasoning pass (see app/services/scoring.py) -- only imported
# when LLM_PROVIDER=openai
openai>=1.50
